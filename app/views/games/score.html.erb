<div class="container">
  <h1>Result</h1>
  <% if @found && @exist %>
  <p>
  <strong>Congratulations!</strong> <%= @word.upcase %> is a valid English word!
  </p>
  <% elsif @exist && !@found %>
  <p>
    Sorry, but <strong><%= @word.upcase %></strong> does not seem to be a valid English word...
  </p>
  <% else %>
  <p>
    Sorry but <strong><%= @word.upcase %></strong> cannot be build out of <%= @letters.split(" ").join(", ") %>
  </p>
  <% end %>

  <%= link_to "PLAY AGAIN", new_path %>
</div>
